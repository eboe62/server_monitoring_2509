# requirements.txt

# Core runtime dependencies
psycopg2-binary>=2.9.9      # Conexión PostgreSQL
python-dotenv>=1.0.1        # Cargar variables desde .env
requests>=2.31.0            # Cliente HTTP (usado en geolocalización)

# Logging / formatting
jinja2>=3.1.3               # Formateo HTML

# Observability / parsing
geoip2>=4.8.0               # Geolocalización de IP's

# Scheduling / misc
schedule>=1.2.1             # Ejecución periódica dentro de scripts
